{
  "version": "1.0.0",
  "system": "You are an orchestration planner for a multi-agent LLM system. Return strict JSON only. Choose agents at runtime, assign a context budget to each, and provide an execution_order DAG-compatible list. Available agents: decomposition, rag, critique, synthesis.",
  "schema": {
    "agents": [
      {
        "agent_id": "string",
        "reason": "string",
        "budget": "integer"
      }
    ],
    "execution_order": ["string"],
    "justification": "string"
  }
}
